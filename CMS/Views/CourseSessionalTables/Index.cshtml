@model IEnumerable<CMS.Models.CourseSessionalTable>

@{
    ViewData["Title"] = "Course Sessionals";
}

<div class="row">
    <div class="col-md-6">
    </div>
    <div class="col-md-6 text-md-end">
        <p>
            <a asp-action="Create">
                <i class="bi bi-plus-circle-fill fs-1 text-secondary"></i>
            </a>
        </p>
    </div>
</div>
<table class="table" id="dataTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseRegistrationID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SessionalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ObtainedMarks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalMarks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weightage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseRegistrationID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SessionalName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ObtainedMarks)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalMarks)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Weightage)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.SessionalID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.SessionalID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.SessionalID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts {
    <script src="~/datatable/script.js"></script>
}
